# =============================================================================
# ANYSCHOOL BACKEND - ENVIRONMENT VARIABLES TEMPLATE
# =============================================================================
# 
# INSTRUCTIONS:
# 1. Copy this file to .env (NOT committed to git)
# 2. Fill in your actual values
# 3. Source it before running: source .env
# 4. OR use your IDE's environment variable configuration
#
# =============================================================================

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/anyschool_db
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=your_database_password_here

# =============================================================================
# JWT CONFIGURATION
# =============================================================================
# IMPORTANT: Generate a strong secret key (minimum 256 bits)
# You can generate one using: openssl rand -base64 32
JWT_SECRET=your-very-long-and-secure-secret-key-here

# =============================================================================
# EMAIL CONFIGURATION (for OTP verification)
# =============================================================================
# Gmail Example (requires App Password if using Gmail):
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password-here

# Other email providers:
# - Outlook: smtp.office365.com:587
# - SendGrid: smtp.sendgrid.net:587
# - AWS SES: email-smtp.region.amazonaws.com:587

# =============================================================================
# NOTES
# =============================================================================
# For production, use a secrets manager (AWS Secrets Manager, HashiCorp Vault, etc.)
# Never commit your actual .env file!
# This template file (.env.template) is safe to commit
#
# Example export commands:
# export SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/anyschool_db
# export SPRING_DATASOURCE_USERNAME=postgres
# export SPRING_DATASOURCE_PASSWORD=your_password
# export JWT_SECRET=your-secret-key
